<% @translation.comments.each do |c| %>
    <div class="comment-content">
        <p> <%= c.title %>, by: <%= c.user.name %>, on: <%= c.created_at.strftime("%m/%d/%Y") %> </p>
        <p> <%= c.content %> </p>
        <p>Rating: <%= c.rating %> </p>
    </div>
<% end %>