<h1><%= @order.name %></h1>
<p>by: <%= link_to  @order.user.name, user_path(@order.user) %> Number of people: <%= @order.people %> --   
Dietary Restrictions: <%= @order.dietary_restrictions %> --
Flavor Preferences: <%= @order.flavor_preferences %> -- 
Description: <%= @order.description  %>
</p>
<ul>
<% @order.dishes.each do |d| %>
    <li><%= d.translated_name %></li>
<% end %>
</ul>

<%= link_to  "Edit", edit_order_path(@order) %>
<%= link_to  "Delete", order_path(@order), method: "delete", data: { confirm: 'Are you sure?' } %>